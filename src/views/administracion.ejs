<main class="contenedor panel-administracion">
  <h1><%= nombrePagina %> </h1>
  <div class="contenedor-botones">
    <a href="/nuevo-grupo" class="btn btn-amarillo">Nuevo Grupo</a>
    <a href="/nuevo-meeti" class="btn btn-azul">Nuevo Meeti</a>
    <a href="/editar-perfil" class="btn btn-rosa">Editar Perfil</a>
    <a href="/cambiar-password" class="btn btn-verde">Cambiar Contraseña</a>
    <a href="/imagen-perfil" class="btn btn-amarillo">Imagen Perfil</a>
  </div>
  <div class="seccion-admin">
    <h2>Próximos Meeti's</h2><!-- Meeti's -->
    <% if(meeti.length){ %>
    <ul>
      <% meeti.forEach(meet => { %>
      <li>
        <div class="informacion-admin">
          <% moment.locale('es'); %>
          <% const fecha = meet.fecha+ ' '+meet.hora %> 
          <p class="fecha"><%= moment(fecha).format('LLLL'); %> Horas</p>
          <a href="/meeti/<%= meet.slug %>">
            <h3><%= meet.titulo %></h3>
          </a>
          <small><%= meet.interesados.length %> asistentes</small>
        </div>
        <div class="acciones contenedor-botones">
          <a href="/editar-meeti/<%= meet.id %>" class="btn btn-verde">Editar</a>
          <a href="/asistentes-meeti/<%= meet.id %>" class="btn btn-azul2">Asistentes</a>
          <a href="/eliminar-meeti/<%= meet.id %>" class="btn btn-rojo">Eliminar</a>
        </div>
      </li>
      <% }) %>
    </ul>
    <% }else{ %>
      <p>Aún no tienes meeti's creados</p>
    <% } %>
  </div>
  <div class="seccion-admin">
    <h2>Tus Grupos</h2>
    <% if(grupos.length){ %> 
      <ul>
        <% grupos.forEach(grupo => { %> 
          <%= //JSON.stringify(grupo) %> 
        <li>
          <div class="informacion-admin">
            <h3><%= grupo.nombre %> </h3>
          </div>
          <div class="acciones contenedor-botones">
            <a href="/editar-grupo/<%= grupo.id %>" class="btn btn-verde">Editar</a>
            <a href="/imagen-grupo/<%= grupo.id %>" class="btn btn-azul2">Imagen</a>
            <a href="/eliminar-grupo/<%= grupo.id %>" class="btn btn-rojo">Eliminar</a>
          </div>
        </li>
      <% }) %> 
      </ul>
    <% }else{ %> 
      <p>No tienes un grupo creado.</p>
    <% } %> 
  </div>
  <!-- <div class="seccion-admin">
    <h2>Meeti's que Asistiras</h2>
    <% if(meetiAsistir.length){ %>
      <ul>
      <% meetiAsistir.forEach(meet => { %>
        <%= JSON.stringify(meet) %> 
        <li>
          <a href="/meeti/<%= meet.slug %>"><%= meet.titulo %> </a>
        </li>
      <% }) %> 
    </ul>
    <% }else{ %>
      <p>Todavía, no estas Asistiendo a ningún meeti</p>
    <% } %> 
  </div> -->
  <div class="seccion-admin">
    <h2>Anteriores Meeti's</h2><!-- Meeti's -->
    <% if(meetiPrevius.length){ %>
    <ul>
      <% meetiPrevius.forEach(meet => { %>
      <li>
        <div class="informacion-admin">
          <% moment.locale('es'); %>
          <% const fecha = meet.fecha+ ' '+meet.hora %> 
          <p class="fecha"><%= moment(fecha).format('LLLL'); %> Horas</p>
          <a href="/meeti/<%= meet.slug %>">
            <h3><%= meet.titulo %></h3>
          </a>
          <small><%= meet.interesados.length %> asistentes</small>
        </div>
        <div class="acciones contenedor-botones">
          <a href="/editar-meeti/<%= meet.id %>" class="btn btn-verde">Editar</a>
          <a href="/asistentes-meeti/<%= meet.id %>" class="btn btn-azul2">Asistentes</a>
          <a href="/eliminar-meeti/<%= meet.id %>" class="btn btn-rojo">Eliminar</a>
        </div>
      </li>
      <% }) %>
    </ul>
    <% }else{ %>
      <p>Aún no tienes meeti's anteriores</p>
    <% } %>
  </div>
</main>